

### 🎯 **一、核心管理器 (Managers)**

#### 1. **GameManager** - 游戏核心管理器
- **作用**: 游戏的总控制器，使用单例模式
- **职责**: 
  - 确保场景中只有一个全局实例
  - 跨场景持久化（DontDestroyOnLoad）
  - 游戏整体流程控制
- **关联**: 无直接依赖，其他管理器独立运行

#### 2. **UIManager** - UI 显示管理器 ⭐
- **作用**: 统一管理所有 UI 元素的显示和更新
- **核心方法**:
  - `UpdateHealth()`: 更新血条 + 生命值数值文本
  - `UpdateLevel()`: 更新等级显示
  - `UpdateXP()`: 更新经验条进度 + 经验数值
  - `UpdateGold()`: 更新金币显示
  - `UpdateWeaponSlots()`: 更新武器槽位图标
- **依赖**: 
  - 需要 UI 元素引用（Slider、Text、Image）
  - 被 `PlayerStats` 调用
- **数据流向**: PlayerStats → UIManager → UI 显示

#### 3. **LevelUpManager** - 升级系统管理器 ⭐
- **作用**: 处理玩家升级和三选一卡片系统
- **核心方法**:
  - `ShowLevelUpUI()`: 暂停游戏，生成 3 张随机卡片
  - `ApplyUpgrade()`: 应用属性卡效果
  - `ApplyNewWeapon()`: 装备新武器
  - `InitializeCard()`: 根据规则智能生成卡片（优先新武器）
- **依赖**:
  - `UpgradeCard_SO` (升级卡片数据)
  - `WeaponData_SO` (武器数据)
  - `PlayerStats` (应用升级效果)
  - `PlayerWeaponManager` (检查已有武器)
- **数据流**: PlayerStats.LevelUp → LevelUpManager → 生成卡片 → 玩家选择 → 应用效果

#### 4. **PoolManager** - 对象池管理器
- **作用**: 优化性能，复用对象避免频繁实例化/销毁
- **核心方法**:
  - `Get()`: 从池中获取对象，没有则创建新的
  - `Return()`: 将对象放回池中（SetActive(false)）
- **使用场景**:
  - 敌人生成/死亡
  - 子弹生成/消失
  - 经验球生成/拾取
- **优势**: 减少 GC（垃圾回收），提升性能

---

### 👤 **二、玩家系统 (Player)**

#### 1. **PlayerStats** - 玩家属性数据中心 ⭐⭐⭐
- **作用**: 管理玩家所有核心属性
- **核心数据**:
  - 生命值：`maxHealth`, `currentHealth`
  - 移动速度：`moveSpeed`
  - 战斗加成：`baseAttackBonus`, `attackSpeedBonus`
  - 升级系统：`currentLevel`, `currentXP`, `xpToNextLevel`
- **核心方法**:
  - `TakeDamage()`: 受伤并更新 UI 血条
  - `AddXP()`: 获得经验并更新 UI 经验条
  - `LevelUp()`: 升级并触发升级 UI
  - `GetFinalDamage()`: 计算最终伤害（含攻击力加成）
  - `GetAttackSpeedMultiplier()`: 获取攻速倍率
- **依赖**: 
  - 强依赖 `UIManager` 进行 UI 更新
  - 触发 `LevelUpManager` 显示升级界面
- **关键逻辑**: 所有属性变化都会立即同步到 UI

#### 2. **PlayerController** - 玩家控制器
- **作用**: 处理玩家移动输入
- **核心方法**:
  - `Update()`: 获取 WASD/方向键输入
  - `FixedUpdate()`: 通过 Rigidbody2D 实现物理移动
- **依赖**: 
  - `PlayerStats` (获取移动速度)
  - `Rigidbody2D` (物理组件)
- **特点**: 使用 normalized 保证斜向移动不会更快

#### 3. **PlayerWeaponManager** - 玩家武器管理器 ⭐
- **作用**: 管理玩家已装备的武器（最多 4 个）
- **核心方法**:
  - `EquipWeapon()`: 装备新武器并通知 UI 更新
  - 初始化时自动装备起始武器
- **依赖**:
  - `WeaponData_SO` (武器配置数据)
  - `WeaponBase` (武器基类)
  - `UIManager` (更新武器槽位显示)
- **数据流**: 获得武器 → 实例化 → 初始化 → 添加到列表 → 更新 UI

#### 4. **ExperienceOrb** - 经验球
- **作用**: 可拾取的经验值道具
- **核心方法**:
  - `OnTriggerEnter2D()`: 检测玩家触碰，给予经验后返回对象池
- **依赖**:
  - `PlayerStats` (添加经验)
  - `PoolManager` (对象池回收)
- **生成方式**: EnemyAI.Die() → DropXP() → PoolManager.Get()

---

### 🔫 **三、武器系统 (Weapons)**

#### 1. **WeaponBase** - 武器基类 ⭐⭐
- **作用**: 所有武器的抽象基类，定义通用逻辑
- **核心机制**:
  - `Initialize()`: 动态注入武器数据和玩家引用
  - `Update()`: 自动按攻速冷却调用 Attack()
  - `CalculateCooldown()`: 根据基础攻速和玩家攻速加成计算实际冷却
- **设计模式**: 模板方法模式
- **安全机制**: isInitialized 开关防止未初始化就运行
- **子类需实现**: `Attack()` 抽象方法

#### 2. **具体武器实现** (继承自 WeaponBase):

**VacuumWeapon (吸尘器)**:
- 特性：范围吸附 + 持续伤害
- `PullEnemies()`: 每帧拉扯范围内敌人
- `Attack()`: 周期性对范围内所有敌人造成伤害

**WaterGunWeapon (水枪)**:
- 特性：发射子弹
- 使用 `WaterBullet` 预制体

**LawnmowerWeapon (割草机)**:
- 特性：近战旋转刀片
- 使用 `LawnmowerBlade` 旋转攻击

**TongsWeapon (垃圾钳)**:
- 特性：远程夹取

**SprayWeapon (喷雾器)**:
- 特性：扇形范围攻击

**SnowplowWeapon (推雪铲/扫帚)**:
- 特性：前方推进攻击
- 生成 `SnowplowMinion` 跟随物

**TrashCanWeapon (垃圾桶)**:
- 特性：放置型防御
- 使用 `TrashCanLure` 吸引敌人

---

### 👾 **四、敌人系统 (Enemies)**

#### **EnemyAI** - 敌人人工智能 ⭐
- **作用**: 控制敌人行为
- **核心方法**:
  - `Initialize()`: 从对象池取出时初始化数据
  - `Update()`: 追踪玩家移动
  - `OnCollisionStay2D()`: 碰撞时对玩家造成持续伤害
  - `TakeDamage()`: 承受伤害
  - `Die()`: 死亡时掉落经验球
- **依赖**:
  - `EnemyData_SO` (敌人配置数据)
  - `PlayerStats` (对玩家造成伤害)
  - `PoolManager` (死亡后回收)
  - `ExperienceOrb` (生成经验球)
- **数据流**: 生成 → 追踪 → 攻击 → 受伤 → 死亡 → 掉落经验

---

### 🃏 **五、UI 系统**

#### **UpgradeCardUI** - 升级卡片 UI
- **作用**: 显示单个升级卡片的内容
- **核心方法**:
  - `Setup()`: 显示属性卡
  - `SetupWeapon()`: 显示武器卡
  - `OnClick()`: 点击时应用升级效果
- **依赖**:
  - `UpgradeCard_SO` / `WeaponData_SO` (显示内容)
  - `LevelUpManager` (应用效果)
- **组成**: Text(名称) + Image(图标) + Text(描述) + Button(点击)

---

### 📦 **六、ScriptableObject 数据类 (Resources)**

#### 1. **WeaponData_SO** - 武器数据资产 ⭐
- **作用**: 可序列化的武器配置数据
- **字段**:
  - 基本信息：weaponName, icon, description
  - 战斗属性：prefab, baseDamage, baseFireRate
- **优势**: 在 Project 窗口右键创建资产，无需写代码即可配置武器

#### 2. **UpgradeCard_SO** - 升级卡片数据资产 ⭐
- **作用**: 可序列化的升级卡片配置
- **字段**:
  - 卡片信息：cardName, icon, description
  - 奖励规则：rarity, canAppearConsecutively
  - 属性提升：statType, valueType, value
- **枚举**:
  - CardRarity: White(白卡), Blue(蓝卡)
  - StatType: HP/AttackSpeed/MoveSpeed/Damage
  - ValueType: Numeric(数值)/Percentage(百分比)

#### 3. **EnemyData_SO** - 敌人数据资产
- **作用**: 敌人配置数据
- **字段**: maxHP, moveSpeed, xpDrop 等

---

### 🔄 **七、核心数据流和关联关系**

```
┌─────────────────────────────────────────────────────┐
│                    游戏开始                          │
└───────────────────┬─────────────────────────────────┘
                    │
        ┌───────────▼───────────┐
        │    GameManager        │ (全局控制)
        └───────────┬───────────┘
                    │
        ┌───────────▼───────────┐
        │   PlayerStats         │◄────┐ (属性数据)
        └────┬──────┬───────────┘     │
             │      │                 │
    ┌────────▼      ▼────────┐       │
    │PlayerController        │       │
    │(移动)                  │       │
    └────────────────────────┘       │
             │                       │
    ┌────────▼────────┐             │
    │PlayerWeaponMgr  │             │
    │(武器管理)       │             │
    └────────┬────────┘             │
             │                      │
    ┌────────▼────────┐             │
    │WeaponBase       │─────────────┘ (读取玩家属性)
    │(武器基类)       │
    └────────┬────────┘
             │
    ┌────────▼────────┐
    │具体武器         │
    │(Vacuum 等)      │
    └────────┬────────┘
             │
    ┌────────▼────────┐      ┌──────────────┐
    │   EnemyAI       │─────►│PoolManager   │ (对象池)
    │(追踪/攻击/死亡) │      └──────────────┘
    └────────┬────────┘
             │
    ┌────────▼────────┐      ┌──────────────┐
    │ExperienceOrb    │─────►│PlayerStats   │ (添加 XP)
    └────────┬────────┘      └──────┬───────┘
                                  │
                          ┌───────▼────────┐
                          │  LevelUpManager│
                          │(三选一卡片)    │
                          └───────┬────────┘
                                  │
                          ┌───────▼────────┐
                          │ UpgradeCardUI  │
                          └───────┬────────┘
                                  │
                          ┌───────▼────────┐
                          │  PlayerStats   │ (应用升级)
                          └───────┬────────┘
                                  │
                          ┌───────▼────────┐
                          │   UIManager    │ (显示更新)
                          └────────────────┘
```


---

### 🎨 **八、关键设计模式**

1. **单例模式**: GameManager, UIManager, LevelUpManager, PoolManager
2. **模板方法模式**: WeaponBase 定义框架，子类实现 Attack()
3. **工厂模式**: PoolManager.Get() 创建/复用对象
4. **ScriptableObject 架构**: 数据与逻辑分离，便于策划配置
5. **观察者模式**: PlayerStats 变化 → UIManager 更新

---

### ✨ **九、新增功能总结**

**经验条和生命值显示功能的实现**:
- 修改 `UIManager`: 添加 `healthText`, `xpText`, `xpBar` 引用
- 修改 `UIManager.UpdateHealth()`: 同时更新血条和数值文本
- 新增 `UIManager.UpdateXP()`: 更新经验条和经验数值
- 修改 `PlayerStats`: 在 Start/AddXP/LevelUp 时调用 UI 更新

这个架构清晰、模块化强，易于扩展新功能！🎮